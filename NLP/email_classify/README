垃圾邮件进行分类
# 第一步，特征的提取
在data目录下面有两个文件，正常的邮件：ham_data.txt和垃圾邮件：spam_data.txt
classifier.py的get_data方法将这两个文件读入内存
然后给每一个正例打上1的标签，每一个负例打上0的标签
同时是返回正负样本，以及正负样本的标签
正样本:5000
负样本:5001
总的数据量:10000

# 第二步，去除空行的数据
# 第三步:分测试集合和训练集合

# 第四步,将这些数据加载后，需要将这些数据处理，进行标准化，去停用词、分词等操作
使用的是normalization.py函数
# feature_extractors.py函数特征提取的函数
